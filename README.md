# SW 유해성 검증 도구
본 스크립트는 소프트웨어(SW) 반입 전, 보안 체크리스트(엑셀) 작성을 돕기 위해 파일의 세부 정보(제품명, 버전, 제조사 등)와 해시값, 전자서명 상태를 자동 추출하는 도구입니다.

스크립트 실행 시 함께 생성되는 **작성용 엑셀 템플릿(CSV 파일)** 을 열어서 내용만 복사 후, 
**아래 링크 양식의 엑셀에 붙여넣어 작성하고 원본 보안 검증 리포트(TXT)와 함께 제출**해주세요. 
**(반드시 3개 파일 함께 제출)**
https://docs.google.com/spreadsheets/d/1liPC7i6VgYWtiANkSjFFBfXtGZZ04baN4-TfnkH2TNU

## 주요 기능
- **엑셀 템플릿(CSV) 자동 생성**: 사용자가 엑셀 양식에 바로 붙여넣어 편집할 수 있는 체크리스트 초안(`SW_Checklist_Template_*.csv`) 파일이 함께 생성됩니다.
- **해시 자동 추출**: 현재 폴더 및 하위 폴더의 모든 파일에 대해 SHA-256 해시값을 비롯한 파일 정보를 자동으로 추출합니다.
- **전자서명 자동 검증**: 실행 파일(EXE, DLL 등)의 디지털 서명과 서명자 정보를 추출하여 엑셀 양식 항목(`Valid(정상)`, `Invalid(경고)`, `N/A(없음)`)에 맞춰 자동 변환해 줍니다.

## 사용 방법
1. 검증하고자 하는 SW 파일들이 있는 폴더(모든 파일을 하나의 폴더로 모으기)로 `SWvalidator.ps1` 파일을 복사합니다.
2. PowerShell 콘솔에서 스크립트를 실행합니다. 
   (또는 스크립트 파일 우클릭 후 "PowerShell에서 실행" 선택)
   ```powershell
   .\SWvalidator.ps1
   ```
3. 검사가 완료되면 같은 폴더에 아래의 **두 파일**이 생성됩니다:
   - 엑셀 열람용 초안: `SW_Checklist_Template_YYYYMMDD_HHMMSS.csv`
   - 보안 검증 제출용: `SW_Verification_Report_YYYYMMDD_HHMMSS.txt`
4. **생성된 CSV 파일(엑셀 템플릿)의 내용을 복사하여 지정된 엑셀 작성**에 활용합니다. 빈 부분을 마저 채워 제출용 엑셀을 완성합니다.

## 주의 사항
- **폴더 감지 시 자동압축**: 스크립트 실행 경로 내에 하위 폴더가 존재하는 경우, 보안 정책상 압축 제출이 원칙이므로 자동 압축(`.zip`) 및 검사가 진행됩니다. 
  - 자체개발로 인해 별도 구조의 하위 폴더가 있다면 반드시 미리 `.zip` 등으로 깔끔하게 압축한 뒤 스크립트를 다시 실행하십시오.
  - 원본 실행파일 등은 임의로 폴더에 넣지 않고 개별로 분리해야 합니다.

## 결과물 예시 (CSV 기준)
| 파일명 | 제품명 | 버전 | 제조사 | 라이선스 등 | 해시값(SHA-256) | 전자서명 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| app.exe | Antigravity | 1.19.5 | Google | (직접 기입) | 9051BD81... | Valid(정상) / Google LLC |
| data.zip | data.zip | N/A | N/A | (직접 기입) | 3C65DF2B... | N/A(없음) / N/A(없음) |

---
## 생성된 **txt 파일(검증 리포트)**은 조작 여부 확인을 위한 핵심 보안 자료이므로 절대 **수정 또는 새로 저장하지 않은 초기 원본 스크립트 결과 그대로 제출**하세요.
